# P01 — Инфраструктура для работы с агентом

**Статус:** 🚧 в работе
**Цель:** G2 — безопасная и воспроизводимая среда разработки
**Обновлён:** 2026-07-21

## Зачем

Агент должен работать с урезанными правами в изолированном окружении, а не с
полным доступом к рабочей машине. Теория и настройка расписаны в
`docs/agent-security.md` — здесь остаётся довести до реально работающего состояния.

## Готово, когда

- [ ] Агент запускается с токеном `mn-agent`, а не с личным `gh auth login`
- [ ] У агента нет доступа к `~/.ssh` и `~/.config/gh`
- [ ] Любой клон репы поднимается одной командой

## Задачи

### P01-T1 — Урезанный токен 🚧
Пошаговая инструкция готова в `docs/agent-security.md`, осталось выполнить.

- [x] Создать fine-grained токен `mn-agent` (Administration = No access) — 2026-07-21
- [x] Проверить урезанность: `DELETE .../branches/main/protection` → 403 — 2026-07-21
- [ ] Прописать `GH_TOKEN` в окружение агента, убрать личный `gh auth`

Проверка 2026-07-21: `Administration` закрыт полностью — 403 даёт не только
`DELETE`, но и `GET` protection. Токен не может ни прочитать, ни изменить защиту `main`.

Токен лежит в `~/.config/gh/hosts.yml` и используется через `gh auth`, а не через
переменную `GH_TOKEN`. Функционально работает, но агент этот файл **читает** —
пока не будет песочницы (P01-T2), пункт «нет доступа к `~/.config/gh`» не закрыть.

### P01-T2 — Песочница 🔜
Ликбез по теме — `docs/learning-agenda.md` п.2.

- [ ] Разобраться с вариантами изоляции, выбрать под macOS
- [ ] Собрать минимальный `.devcontainer/` — репа примонтирована, в env только `GH_TOKEN`
- [ ] Проверить, что из контейнера не видно `~/.ssh` хоста
- [ ] Записать решение в `ai/context/decisions-log.md`

### P01-T3 — Гигиена репозитория ✅

- [x] pre-push хук через husky, активируется на `pnpm install`
- [x] Branch protection на `main`, работа только через PR
- [x] Правила для агента в `AGENTS.md`
- [ ] Решить: остаться публичным или уйти в приватный + GitHub Pro
- [ ] Опционально: pre-commit с `pnpm lint && pnpm typecheck`

## Заметки

- Решение про публичность репы — сейчас публичный, потому что на Free branch
  protection для приватных недоступна. Пересмотреть, когда появится что скрывать.
- Ликбез по corepack/pnpm (`learning-agenda.md` п.3) к этому проекту не привязан,
  можно разобрать в любую свободную сессию.
