# Бэклог

> **Инбокс.** Сюда падает всё, что прилетело в голову, но пока не разобрано.
> Отсюда мысль уходит либо в задачу внутри проекта (`docs/projects/`), либо
> становится новым проектом, либо удаляется. См. [процесс](./process.md).
>
> Структурированная работа живёт **не здесь**, а в файлах проектов.

## Разобрать

- [ ] _..._

## Кандидаты в проекты

- Реальные приложения вместо заглушек — `@nestjs/cli new`, `create-next-app`,
  `create-expo-app`. Ждёт решения по стеку в P02-T3.
- Модуль в `apps/api` для записи и семантического поиска по `ai_memory`
  (схема готова — `ai/memory/schema.sql`).
- pre-commit хук с `pnpm lint && pnpm typecheck`.

## Отложено (❄️)

- Уход в приватный репозиторий + GitHub Pro — пока нечего скрывать,
  на Free branch protection работает только для публичных.
