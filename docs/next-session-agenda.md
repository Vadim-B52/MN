# Повестка на следующую сессию

> Заполняется **в конце** каждой сессии, пока контекст свежий.
> В начале следующей — по каждому пункту решить: **делаю** / **в бэклог** / **удалить**.
> Держи 1–3 пункта. Больше — это уже не повестка, а бэклог.

**Обновлено:** 2026-07-20

## Взять в работу

1. **P02-T1 — сформулировать идею продукта** (сессия целиком)
   Выписать 3–5 вариантов «боль → кто страдает → почему сейчас», выбрать один,
   заполнить `ai/context/overview.md` и «Видение» в `docs/roadmap.md`.
   Блокирует всё остальное: цели не на чем основывать, стек выбран вслепую.

2. **P01-T1 — создать токен `mn-agent`** (~20 мин)
   Инструкция в `docs/agent-security.md`, включая проверку на 403.
   Делается в вебе под твоим аккаунтом — агент не может, только проверит результат.

## Если останется время

- P01-T2 — разобраться с песочницей, собрать минимальный `.devcontainer/`
  (`docs/learning-agenda.md` п.2)

## Отложено в бэклог

_Пусто._

---

## Контекст прошлой сессии (2026-07-20)

Разобран ликбез по corepack и pnpm — конспект в `docs/pnpm-corepack.md`,
пункт 3 в `learning-agenda.md` закрыт. Поправлен вводящий в заблуждение
комментарий в `.npmrc`: `engine-strict` проверяет только версию Node,
а от `npm install` вместо `pnpm install` не защищает.

Ранее в этот день: настроен процесс работы (`docs/process.md`), заведены
цели G1/G2 и проекты P01/P02, агенту разрешено править файлы и открывать PR
без апрува.

**Открытые вопросы:**
- Продукт не определён — главный блокер, см. P02.
- Стек (NestJS / Next.js / Expo) выбран дефолтом, продуктом не подтверждён.
- Остаться публичным или уйти в приватный + GitHub Pro (~$4/мес)?
- Токена `mn-agent` до сих пор нет — агент работает с полными правами.
